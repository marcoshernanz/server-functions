# Examples

Start with [serverFunctions.ts](./serverFunctions.ts). It shows the public API with both Zod and Valibot schemas.

Then open [policies.ts](./policies.ts) to see the reusable policies that feed `context`.

If you want the tiny fake app behind those examples, open [app.ts](./app.ts).
